{{/*
RHOAI 3.x no longer ships ModelMesh. A ModelMesh InferenceService in
ml-development stays Ready=Unknown forever and blocks later Argo waves
(notebooks). The sklearn model is served by KServe in
industrial-edge-inference and industrial-edge-tst-all.
*/}}
