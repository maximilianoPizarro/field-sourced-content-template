= Streams for Apache Kafka
:navtitle: Streams for Apache Kafka

++++
<div id="scene-kafka" style="width:100%;min-height:400px;"></div>
<script src="https://unpkg.com/three@0.128.0/build/three.min.js"></script>
<script src="https://unpkg.com/three@0.128.0/examples/js/controls/OrbitControls.js"></script>
<script src="../_/js/three-helper.js"></script>
<script>
(function(){
  var ctx = ThreeHelper.initScene('scene-kafka', {height:380, camY:12, camZ:22});
  if(!ctx) return;
  var b0 = ThreeHelper.createNode(ctx.scene,'Broker 0',0xD32F2F,-5,0,0);
  var b1 = ThreeHelper.createNode(ctx.scene,'Broker 1',0xD32F2F,0,0,-4);
  var b2 = ThreeHelper.createNode(ctx.scene,'Broker 2',0xD32F2F,5,0,0);
  var kraft = ThreeHelper.createNode(ctx.scene,'KRaft',0xFF9800,0,3,0,{width:2,height:0.8});
  var cg = ThreeHelper.createNode(ctx.scene,'Consumer Group',0x4CAF50,0,-3,5,{width:3.5,height:0.8});
  var edges = [
    ThreeHelper.createEdge(ctx.scene,b0,b1,0x666666,{speed:0.003}),
    ThreeHelper.createEdge(ctx.scene,b1,b2,0x666666,{speed:0.003}),
    ThreeHelper.createEdge(ctx.scene,b2,b0,0x666666,{speed:0.003}),
    ThreeHelper.createEdge(ctx.scene,kraft,b0,0xFF9800,{speed:0.005}),
    ThreeHelper.createEdge(ctx.scene,kraft,b1,0xFF9800,{speed:0.005}),
    ThreeHelper.createEdge(ctx.scene,kraft,b2,0xFF9800,{speed:0.005}),
    ThreeHelper.createEdge(ctx.scene,b1,cg,0x4CAF50,{speed:0.008})
  ];
  ThreeHelper.animate(ctx, edges);
})();
</script>
++++

== ¿Qué es Streams for Apache Kafka?

Distribución self-managed de Apache Kafka diseñada para crear una experiencia superior de instalación, configuración y gestión en *Red Hat OpenShift*.

Basada en el proyecto open source https://strimzi.io/[Strimzi] (CNCF Incubation project), proporciona:

* Container images para Apache Kafka
* Operators para gestionar clusters, topics y usuarios
* HTTP Bridge para Apache Kafka
* Consola UI (StreamsHub)

== Apache Kafka — Fundamentos

Apache Kafka es un sistema distribuido de streaming de datos basado en mecanismo publish-subscribe.

*Características principales:*

* Escalabilidad horizontal
* Tolerancia a fallos
* Datos inmutables (append-only log)
* Open source (Apache License 2.0)

*Casos de uso:*

* Recomendaciones en tiempo real
* Aplicaciones IoT
* Data gathering para IA
* *Change Data Capture (CDC)*

== Operadores Strimzi

[mermaid]
....
graph TB
    CO["Cluster Operator"]
    KC["Kafka CR"]
    TO["Topic Operator"]
    UO["User Operator"]
    EO["Entity Operator"]
    B1["Broker 1"]
    B2["Broker 2"]
    B3["Broker 3"]
    CTRL["Controller<br/>(KRaft)"]

    CO -->|reconcilia| KC
    KC --> EO
    EO --> TO
    EO --> UO
    KC --> B1
    KC --> B2
    KC --> B3
    KC --> CTRL
....

El Cluster Operator observa el CR `Kafka` y reconcilia el estado deseado. Los Topic y User Operators corren como contenedores dentro del Entity Operator.

== Despliegue KRaft (sin Zookeeper)

A partir de Streams for Apache Kafka 3.x, el metadata se almacena *dentro de Kafka* usando KRaft, eliminando la dependencia de Zookeeper.

[source,yaml]
----
apiVersion: kafka.strimzi.io/v1beta2
kind: Kafka
metadata:
  name: cdc-cluster
  namespace: kafka-cdc
spec:
  kafka:
    version: "4.0.0"
    replicas: 3
    listeners:
      - name: plain
        port: 9092
        type: internal
        tls: false
      - name: tls
        port: 9093
        type: internal
        tls: true
    config:
      offsets.topic.replication.factor: 3
      transaction.state.log.replication.factor: 3
      transaction.state.log.min.isr: 2
      default.replication.factor: 3
      min.insync.replicas: 2
    storage:
      type: persistent-claim
      size: 10Gi
      deleteClaim: false
    metricsConfig:
      type: jmxPrometheusExporter
      valueFrom:
        configMapKeyRef:
          name: kafka-metrics-config
          key: kafka-metrics-config.yml
  entityOperator:
    topicOperator: {}
    userOperator: {}
  kafkaExporter:
    topicRegex: ".*"
    groupRegex: ".*"
----

=== Cluster KRaft: 3 nodos

++++
<div id="kraft-cluster-3d" class="three-container" style="height:400px;"></div>
<script>
(function() {
  const container = document.getElementById('kraft-cluster-3d');
  if (!container) return;

  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(50, container.clientWidth / container.clientHeight, 0.1, 1000);
  const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
  renderer.setSize(container.clientWidth, container.clientHeight);
  renderer.setClearColor(0x0a0a0a);
  container.appendChild(renderer.domElement);

  var fsBtn = document.createElement('button');
  fsBtn.className = 'fullscreen-btn';
  fsBtn.textContent = '\u26F6 Fullscreen';
  fsBtn.onclick = function() {
    if (container.requestFullscreen) container.requestFullscreen();
    else if (container.webkitRequestFullscreen) container.webkitRequestFullscreen();
  };
  container.appendChild(fsBtn);

  const brokers = [];
  const angleStep = (2 * Math.PI) / 3;
  const radius = 3;

  for (let i = 0; i < 3; i++) {
    const x = Math.cos(angleStep * i) * radius;
    const z = Math.sin(angleStep * i) * radius;
    const geo = new THREE.CylinderGeometry(0.8, 0.8, 2, 6);
    const mat = new THREE.MeshPhongMaterial({ color: 0xCC0000, transparent: true, opacity: 0.8 });
    const mesh = new THREE.Mesh(geo, mat);
    mesh.position.set(x, 0, z);
    scene.add(mesh);
    brokers.push(mesh);

    const canvas = document.createElement('canvas');
    canvas.width = 128; canvas.height = 48;
    const ctx = canvas.getContext('2d');
    ctx.fillStyle = '#fff';
    ctx.font = 'bold 18px Arial';
    ctx.textAlign = 'center';
    ctx.fillText('Broker ' + i, 64, 32);
    const texture = new THREE.CanvasTexture(canvas);
    const spriteMat = new THREE.SpriteMaterial({ map: texture });
    const sprite = new THREE.Sprite(spriteMat);
    sprite.position.set(x, 1.8, z);
    sprite.scale.set(2, 0.6, 1);
    scene.add(sprite);
  }

  for (let i = 0; i < 3; i++) {
    for (let j = i + 1; j < 3; j++) {
      const points = [brokers[i].position.clone(), brokers[j].position.clone()];
      const geo = new THREE.BufferGeometry().setFromPoints(points);
      const mat = new THREE.LineBasicMaterial({ color: 0x00ff88, transparent: true, opacity: 0.5 });
      scene.add(new THREE.Line(geo, mat));
    }
  }

  const ctrlGeo = new THREE.OctahedronGeometry(0.6);
  const ctrlMat = new THREE.MeshPhongMaterial({ color: 0x0066CC });
  const ctrl = new THREE.Mesh(ctrlGeo, ctrlMat);
  ctrl.position.set(0, 2.5, 0);
  scene.add(ctrl);

  const ctrlCanvas = document.createElement('canvas');
  ctrlCanvas.width = 128; ctrlCanvas.height = 48;
  const ctrlCtx = ctrlCanvas.getContext('2d');
  ctrlCtx.fillStyle = '#0066CC';
  ctrlCtx.font = 'bold 16px Arial';
  ctrlCtx.textAlign = 'center';
  ctrlCtx.fillText('KRaft Controller', 64, 32);
  const ctrlTex = new THREE.CanvasTexture(ctrlCanvas);
  const ctrlSprite = new THREE.Sprite(new THREE.SpriteMaterial({ map: ctrlTex }));
  ctrlSprite.position.set(0, 3.8, 0);
  ctrlSprite.scale.set(2.5, 0.6, 1);
  scene.add(ctrlSprite);

  scene.add(new THREE.AmbientLight(0x404040, 2));
  const dl = new THREE.DirectionalLight(0xffffff, 1);
  dl.position.set(5, 5, 5);
  scene.add(dl);

  camera.position.set(0, 4, 8);
  camera.lookAt(0, 0.5, 0);

  let t = 0;
  function animate() {
    requestAnimationFrame(animate);
    t += 0.008;
    brokers.forEach((b, i) => { b.rotation.y = t + i * 2; });
    ctrl.rotation.y = -t * 2;
    ctrl.rotation.x = Math.sin(t) * 0.3;
    renderer.render(scene, camera);
  }
  animate();
})();
</script>
++++

== How it Works

=== KRaft Consensus (sin Zookeeper)

A partir de Kafka 3.x, el metadata del cluster se gestiona internamente usando el protocolo *KRaft* (Kafka Raft):

. Cada broker tiene un rol: *controller* (gestión de metadata) o *broker* (almacenamiento y serving de datos), o ambos en clusters pequeños (combined mode).
. Los controllers forman un _quorum_ usando Raft. Un controller es elegido como *active controller* — es el único que escribe metadata.
. Cuando un topic se crea o una partición se reasigna, el active controller escribe el cambio en un log interno (`__cluster_metadata`), que se replica a los otros controllers.
. Los brokers obtienen el metadata suscribiéndose al log del controller — no hay polling, es push-based.
. Si el active controller cae, los controllers restantes eligen un nuevo líder via Raft en *milisegundos*, eliminando el delay de minutos que ocurría con Zookeeper.

=== Ciclo de vida de un mensaje

. Un *producer* envía un batch de registros al broker *leader* de la partición destino.
. El leader escribe el batch en su log local (append-only en disco, sequential I/O).
. Los *followers* (réplicas) fetch el batch del leader y lo escriben en sus propios logs.
. Cuando `min.insync.replicas` réplicas (incluyendo el leader) confirman el write, el leader envía un *ACK* al producer.
. El offset del batch se asigna secuencialmente — es inmutable y monotónicamente creciente.
. Un *consumer* con un _consumer group_ asignado hace `poll()` al leader de cada partición asignada, recibiendo batches desde su último offset committed.

=== Strimzi Reconciliation Loop

El Cluster Operator de Strimzi opera como un _Kubernetes controller_ clásico:

. Observa los cambios en los CRs `Kafka`, `KafkaConnect`, `KafkaTopic` y `KafkaUser`.
. Calcula la diferencia entre el estado deseado (CR) y el estado actual (StatefulSets, ConfigMaps, Secrets).
. Aplica los cambios de forma *rolling* — nunca detiene todos los brokers simultáneamente.
. Los Topic y User Operators corren como sidecars en el _Entity Operator_ pod y reconcilian sus respectivos CRs contra el API de Kafka directamente (no Kubernetes resources).

== Kafka Bridge (HTTP REST)

El Kafka Bridge permite producir y consumir mensajes via HTTP REST — ideal para demos y testing sin cliente Kafka nativo:

[source,yaml]
----
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaBridge
metadata:
  name: cdc-bridge
  namespace: kafka-cdc
spec:
  replicas: 1
  bootstrapServers: cdc-cluster-kafka-bootstrap:9092
  http:
    port: 8080
----

*Ejemplo — producir un mensaje:*

[source,bash]
----
curl -X POST https://kafka-bridge-kafka-cdc.apps.<domain>/topics/cdc.public.customers \
  -H "Content-Type: application/vnd.kafka.json.v2+json" \
  -d '{"records":[{"value":{"first_name":"Test","last_name":"User","email":"test@demo.io"}}]}'
----

== Seguridad: Autenticación y Encriptación

El cluster Kafka expone dos listeners: `plain` (9092, sin TLS) y `tls` (9093, con TLS). Los clientes de producción deben usar el listener TLS con autenticación SCRAM-SHA-512.

=== KafkaUser con SCRAM-SHA-512

Strimzi gestiona las credenciales automáticamente al crear un recurso `KafkaUser`. El operador genera un `Secret` con la contraseña:

[source,yaml]
----
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaUser
metadata:
  name: cdc-user
  namespace: kafka-cdc
  labels:
    strimzi.io/cluster: cdc-cluster
spec:
  authentication:
    type: scram-sha-512
  authorization:
    type: simple
    acls:
      - resource:
          type: topic
          name: cdc
          patternType: prefix
        operations: [Read, Write, Describe]
      - resource:
          type: topic
          name: dlq
          patternType: prefix
        operations: [Read, Write, Describe]
      - resource:
          type: group
          name: cdc-connect-cluster
          patternType: literal
        operations: [Read]
      - resource:
          type: group
          name: camel-cdc-consumer
          patternType: literal
        operations: [Read]
----

Las ACLs restringen el acceso: solo los topics con prefijo `cdc` y `dlq`, y los consumer groups específicos del pipeline.

=== Configuración de cliente — KafkaConnect

KafkaConnect se configura para usar el listener TLS (9093) con SCRAM-SHA-512:

[source,yaml]
----
spec:
  bootstrapServers: cdc-cluster-kafka-bootstrap:9093
  authentication:
    type: scram-sha-512
    username: cdc-user
    passwordSecret:
      secretName: cdc-user
      password: password
  tls:
    trustedCertificates:
      - secretName: cdc-cluster-cluster-ca-cert
        certificate: ca.crt
----

Strimzi genera automáticamente el secret `cdc-cluster-cluster-ca-cert` con el certificado CA del cluster.

=== Configuración de cliente — Apache Camel

Camel usa propiedades SASL/SSL en los parámetros del componente `kafka`:

[source,yaml]
----
parameters:
  brokers: cdc-cluster-kafka-bootstrap.kafka-cdc.svc:9093
  securityProtocol: SASL_SSL
  saslMechanism: SCRAM-SHA-512
  saslJaasConfig: "org.apache.kafka.common.security.scram.ScramLoginModule required username=\"${env:KAFKA_USER}\" password=\"${env:KAFKA_PASSWORD}\";"
  sslTruststoreLocation: /etc/kafka-certs/ca.crt
  sslTruststoreType: PEM
----

Las credenciales se inyectan como variables de entorno desde el Secret generado por Strimzi.

== Ecosistema Streams for Apache Kafka

[cols="1,2",options="header"]
|===
| Componente | Rol en el ecosistema

| *Kafka Core* | Brokers, topics, particiones, replicación
| *Kafka Connect* | Framework de connectors (source/sink)
| *Kafka Bridge* | REST proxy HTTP para producir/consumir
| *Apicurio Registry* | Schema Registry (Avro, JSON Schema, Protobuf)
| *Debezium* | CDC connectors para PostgreSQL, MySQL, MongoDB, etc.
| *Streams Console* | UI web para monitorear clusters, topics y consumer groups
| *Kafka Exporter* | Exporta métricas de lag de consumer groups a Prometheus
| *Mirror Maker 2* | Replicación cross-cluster
| *Kroxylicious* | Kafka Proxy — encryption at rest, schema validation
|===

== Documentación Oficial

* https://docs.redhat.com/en/documentation/red_hat_streams_for_apache_kafka[Red Hat Streams for Apache Kafka^] — Guía de instalación, configuración y operación
* https://strimzi.io/documentation/[Strimzi Documentation^] — Proyecto upstream, operadores y CRDs
* https://strimzi.io/blog/[Strimzi Blog^] — Novedades, KRaft migration y mejores prácticas
* https://kafka.apache.org/documentation/[Apache Kafka Documentation^] — Referencia oficial del proyecto Kafka
* https://docs.redhat.com/en/documentation/red_hat_streams_for_apache_kafka/2.9/html/using_the_streams_for_apache_kafka_bridge/index[Kafka Bridge^] — HTTP REST proxy para Kafka
