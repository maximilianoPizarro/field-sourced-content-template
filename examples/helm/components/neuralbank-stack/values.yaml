# --- Configuración Global ---
global:
  postgresql:
    auth:
      postgresPassword: "supersecretpassword"
      username: "neuralbank"
      password: "neuralbank123!"
      database: "neuralbank"

# --- Backend ---
backend:
  image:
    repository: quay.io/maximilianopizarro/neuralbank-backend
    tag: "latest"
    pullPolicy: Always
  service:
    port: 8080
  replicaCount: 1

# --- Frontend ---
frontend:
  image:
    repository: quay.io/maximilianopizarro/neuralbank-front 
    tag: "pkce"
    pullPolicy: Always
  service:
    port: 8080
  replicaCount: 1


# --- Keycloak Configuration (frontend Helm) ---
# Domain replaced by install.sh/Ansible; clientSecret by playbook from Keycloak client "neuralbank-frontend"
keycloak:
  issuerUrl: 'https://rhbk./realms/neuralbank'
  clientId: 'neuralbank-frontend'
  clientSecret: ''
  authorizationEndpoint: 'https://rhbk./realms/neuralbank/protocol/openid-connect/auth'
  redirectUri: 'https://neuralbank./auth/callback'
  tokenEndpoint: 'https://rhbk./realms/neuralbank/protocol/openid-connect/token'
  postLogoutRedirectUri: 'https://neuralbank.'

# --- API Configuration ---
api:
  baseUrl: '/api'