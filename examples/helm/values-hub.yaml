# =============================================================================
# DEPRECATED — not loaded by RHDP or the Pattern CR.
# Source of truth: /values-hub.yaml at the repository root (clusterGroup schema).
# =============================================================================
