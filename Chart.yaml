apiVersion: v2
name: event-driven-architecture
description: Event-Driven Architecture workshop Validated Pattern (hub-only, no ACM/Vault)
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: clustergroup
    version: "0.9.*"
    repository: https://charts.validatedpatterns.io
maintainers:
  - name: Maximiliano Pizarro
    email: mapizarr@redhat.com
