= Event-Driven Architecture Live

++++
<div id="scene-index" style="width:100%;min-height:400px;"></div>
<script src="https://unpkg.com/three@0.128.0/build/three.min.js"></script>
<script src="https://unpkg.com/three@0.128.0/examples/js/controls/OrbitControls.js"></script>
<script src="../../_/js/three-helper.js"></script>
<script>
(function(){
  var ctx = ThreeHelper.initScene('scene-index', {height:420, camX:0, camY:12, camZ:25});
  if(!ctx) return;
  var pg = ThreeHelper.createNode(ctx.scene,'PostgreSQL',0x336791,-10,0,0);
  var deb = ThreeHelper.createNode(ctx.scene,'Debezium',0xE53935,-5,0,0);
  var kf = ThreeHelper.createNode(ctx.scene,'Kafka',0x231F20,0,0,0);
  var cam = ThreeHelper.createNode(ctx.scene,'Camel',0xF57C00,5,0,-3);
  var mail = ThreeHelper.createNode(ctx.scene,'Mailpit',0x42A5F5,10,0,-3);
  var sf = ThreeHelper.createNode(ctx.scene,'SonataFlow',0x0066CC,5,0,3);
  var api = ThreeHelper.createNode(ctx.scene,'Apicurio',0x1A73E8,0,3,4);
  var graf = ThreeHelper.createNode(ctx.scene,'Grafana',0xF46800,-5,3,4);
  var edges = [
    ThreeHelper.createEdge(ctx.scene,pg,deb,0x336791,{speed:0.008}),
    ThreeHelper.createEdge(ctx.scene,deb,kf,0xE53935,{speed:0.01}),
    ThreeHelper.createEdge(ctx.scene,kf,cam,0xF57C00,{speed:0.007}),
    ThreeHelper.createEdge(ctx.scene,cam,mail,0x42A5F5,{speed:0.009}),
    ThreeHelper.createEdge(ctx.scene,kf,sf,0x0066CC,{speed:0.006}),
    ThreeHelper.createEdge(ctx.scene,kf,api,0x1A73E8,{speed:0.005}),
    ThreeHelper.createEdge(ctx.scene,kf,graf,0xF46800,{speed:0.004})
  ];
  ThreeHelper.animate(ctx, edges);
})();
</script>
++++
:page-layout: home

Welcome to *Event-Driven Architecture Live* — a hands-on, real-time Change Data Capture (CDC) pipeline running on Red Hat OpenShift.

This environment wires a production-style event backbone: relational data changes are captured at the database, streamed through Apache Kafka, processed by integration and workflow services, and observed end-to-end. Use the steps below to follow the data from insert to notification and beyond.

== What's running

[cols="1,2,1", options="header"]
|===
| Component | Role | Access

| PostgreSQL
| Source database with sample `customers` and `orders` data (simulated inserts on a schedule)
| Cluster-internal service (`cdc-postgresql` in `kafka-cdc`)

| Debezium
| PostgreSQL CDC connector reading the logical replication stream (WAL) and publishing change events
| Managed via Kafka Connect; inspect tasks in link:https://kafka-console-kafka-cdc.{cluster_domain}[Kafka Console^]

| Kafka (AMQ Streams / Strimzi)
| Durable log for CDC topics, DLQs, and Connect internal topics
| link:https://kafka-console-kafka-cdc.{cluster_domain}[Kafka Console^]

| Apache Camel
| Integration routes that transform and route events (e.g. notifications, side effects)
| Routes run in the Camel CDC workload; topics visible in the console

| Mailpit
| SMTP capture UI for HTTP sink notifications from the pipeline
| link:https://n8n-mailpit-openshift-lightspeed.{cluster_domain}[Mailpit UI^]

| SonataFlow (Kogito BPM)
| BPMN-driven workflow (e.g. customer onboarding) consuming pipeline events
| link:https://customer-onboarding-kogito-bpm.{cluster_domain}[Workflow API^]

| Apicurio Registry
| Schema registry for JSON/Avro-style artifacts and API metadata
| link:https://apicurio-registry-kafka-cdc.{cluster_domain}[Registry UI^]

| Grafana
| Dashboards and metrics for brokers, Connect, and pipeline health
| link:https://grafana-observability.{cluster_domain}[Grafana^]

| Red Hat Developer Hub
| Self-service portal, software templates, and golden paths
| link:https://backstage-developer-hub-developer-hub.{cluster_domain}[Developer Hub^]
|===

== Demo steps

Work through the guides in order. Each page focuses on one slice of the platform so you can see cause and effect clearly.

. xref:01-architecture.adoc[Architecture & Components] — End-to-end flow, partitions, Connect, retention
. xref:02-trigger-cdc.adoc[Trigger a CDC event] — Drive the database and watch the pipeline react
. xref:03-kafka-events.adoc[Kafka & partitions] — Topics, ordering, and the Kafka Console
. xref:04-schemas.adoc[Schema Registry] — Schemas and compatibility in Apicurio
. xref:05-bpm-workflow.adoc[BPM workflow] — SonataFlow and business process automation
. xref:06-monitoring.adoc[Observability] — Grafana and pipeline metrics
. xref:07-security.adoc[Security & governance] — AuthN/Z and operational guardrails
. xref:08-reliability.adoc[Reliability & DRP] — Resilience patterns and recovery
. xref:09-operations.adoc[Operations] — Day-2 tasks for the demo stack
. xref:10-devspaces.adoc[Self-service IDE] — Cloud IDE and developer experience

== Terminal

The showroom embeds a *terminal* panel so you can run `oc`, `kubectl`, and other CLI tools without leaving the guide.

* Open the embedded terminal from the showroom chrome (path: `/terminal/`). It appears on the *right* side of the showroom layout.
* Use it to inspect namespaces (for example `kafka-cdc`), follow pod logs, port-forward services, or verify routes — while keeping the documentation and UIs open beside you.

== Kafka cluster (high level)

The Strimzi `Kafka` custom resource defines the brokers, listeners, and replication settings used by this demo. The snippet below illustrates the shape of the cluster definition (version and storage may match your deployed release).

[source,yaml]
----
apiVersion: kafka.strimzi.io/v1beta2
kind: Kafka
metadata:
  name: cdc-cluster
  namespace: kafka-cdc
spec:
  kafka:
    version: 3.7.0
    replicas: 3
    listeners:
      - name: plain
        port: 9092
        type: internal
        tls: false
    storage:
      type: persistent-claim
      size: 10Gi
      deleteClaim: false
    config:
      offsets.topic.replication.factor: 3
      transaction.state.log.replication.factor: 3
      default.replication.factor: 3
      min.insync.replicas: 2
----
